import { themeTokens } from '@/theme';

const AppColors = {
  primary: themeTokens.colors.brand.emerald,
  background: themeTokens.colors.light.surface,
  text: themeTokens.colors.brand.navy,
  muted: themeTokens.colors.light.textSecondary,
  border: themeTokens.colors.light.border,
};

export default AppColors;
